{
  "name": "finance-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Joseph's monthly finance dashboard — static site on Cloudflare Pages, gated by Cloudflare Access.",
  "scripts": {
    "dev": "python3 -m http.server 4124",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R data dist/",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=finance-dashboard --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
